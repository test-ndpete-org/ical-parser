machine:
  java:
    version: openjdk7
  services:
    - docker

general:
  build_dir: ical

dependencies:
  cache_directories:
    - "~/.m2/repository"
  pre:
    - cp ../cicd/settings.xml ~/.m2/settings.xml
    # - mvn install -U -Dmaven.test.skip=true
